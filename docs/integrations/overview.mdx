---
title: Integrations
description: How Trace connects to agents, editors, and CI.
---

Trace has a clean adapter architecture. Each integration lives in its own folder
under `integrations/` and connects to the **local daemon**. None of them require
the cloud, and none require an API key.

| Integration | Mechanism | Config it patches |
| --- | --- | --- |
| [Claude Code](/integrations/claude) | Session, command, and edit hooks | `~/.claude/settings.json` |
| [Codex](/integrations/codex) | Codex lifecycle hooks + wrapper fallback | `~/.codex/hooks.json` |
| [Cursor](/integrations/cursor) | Session, command, and edit hooks + MCP | `~/.cursor/hooks.json` and `~/.cursor/mcp.json` |
| [Windsurf](/integrations/windsurf) | MCP server | `~/.codeium/windsurf/mcp_config.json` |
| [OpenCode](/integrations/opencode) | V1/V2 guard plugin + MCP | `~/.config/opencode/opencode.json` |
| [GitHub](/integrations/github) | Actions + direct repo read | none (token from env / `gh` / file) |

## One command to connect them all

```bash
trc integrations install all
```

This installs every installable agent integration, writing each hook or MCP file
under `~/.trace/integrations/` and idempotently patching the agent's own config
file. It backs up any file it touches first, starts the local daemon, and never
removes an entry you already configured, so re-running it is safe. Install one
at a time with
`trc integrations install <agent>`, where `<agent>` is `claude`, `codex`,
`cursor`, `windsurf`, or `opencode`.

Check what is live:

```bash
trc integrations status
```

This reports the daemon, whether a GitHub token is available, and which agents are
connected right now. It reads the same config files the installer patches; it does
not guess from an installed binary alone.

## How the adapters differ

Claude Code, Codex, Cursor, and OpenCode can check shell commands before they run.
Their file review is after the edit. Windsurf and Copilot can show activity and
file changes, but do not currently expose the same command hook. An unrecognized
command falls back to a generic terminal adapter.

## Honest boundaries

- Terminal agents launched through `trc run` are wrapped directly and fully
  attributed.
- GUI tools are observed via file changes and git diffs. **Full command guarding
  requires supported hooks or MCP integration points, or running through the
  wrapper.** See [Limitations](/limitations).
- Integrations send only sanitized summaries off-machine, and only the GitHub
  integration goes off-machine at all. They never upload raw project files, raw
  secrets, or the local SQLite database. See [Security Model](/security-model).
