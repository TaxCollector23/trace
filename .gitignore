# Rust
/target
**/*.rs.bk

# Node / Vite
**/node_modules
apps/landing/dist
apps/docs/dist
**/*.tsbuildinfo

# Note: apps/web/dist IS committed. It is embedded into the daemon binary by
# rust-embed, so committing the built dashboard means a fresh clone serves a
# working UI without first running the web build. CI rebuilds it before release.

# Tooling
.vercel
packages/npm/bin/trg
packages/npm/bin/trg.exe

# OS / editor
.DS_Store
*.log
