{
  "name": "trace-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Trace native desktop shell (Tauri) wrapping the local dashboard.",
  "scripts": {
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
