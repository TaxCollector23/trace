{
  "_comment": "Add this to your Claude Code settings.json (merge with existing 'hooks'). Adjust the absolute path to traceguard-hook.sh.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.traceguard/integrations/claude/traceguard-hook.sh"
          }
        ]
      }
    ]
  }
}
