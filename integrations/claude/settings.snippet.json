{
  "_comment": "Add this to your Claude Code settings.json (merge with existing 'hooks'). Adjust the absolute path to trace-hook.sh.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.trace/integrations/claude/trace-hook.sh"
          }
        ]
      }
    ]
  }
}
